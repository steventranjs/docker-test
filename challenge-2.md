# Challenge 2 

1. Containerize the validator project using a Dockerfile.

2. Set up a GitHub Actions workflow to:
- Trigger on every push to main branch.
- Build a Docker image of the project.
- Tag the image with the commit SHA.
- Push the image to container repository (GHCR)